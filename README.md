# Deploy Elixir apps to the cloud with Terraform

## Usage

1. Add this as submodule: `git submodule add git@github.com:yurikoval/ex_infra.git infra`
2. `cd aws` - enter directory with desired config and follow README there.

[AWS](aws)
